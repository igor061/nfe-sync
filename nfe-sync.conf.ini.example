# Configuracao do nfe-sync
# Copie este arquivo para nfe-sync.conf.ini e preencha com seus dados.
# Cada secao representa uma empresa. O nome da secao e usado na CLI (ex: nfe-sync consultar-nsu MINHAEMPRESA).
#
# Dica: use o comando abaixo para preencher automaticamente os dados cadastrais:
#   api_cli cnpjws <cnpj> --salvar-ini MINHAEMPRESA
# Depois preencha apenas path e senha do certificado.

[MINHAEMPRESA]
# Certificado digital A1 (.pfx ou .p12)
path = certs/certificado.pfx
senha = senha_do_certificado

# UF da empresa (sigla em minusculo)
uf = sp

# true = homologacao (testes), false = producao
homologacao = true

# CNPJ da empresa (somente numeros)
cnpj = 00000000000191
